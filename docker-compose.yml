services:
  autohh:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: autohh
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    environment:
      - DATA_DIR=/app/data
    restart: unless-stopped
