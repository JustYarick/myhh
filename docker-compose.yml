services:
  autohh:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: autohh
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    environment:
      - BROWSER_HEADLESS=true
      - DATA_DIR=/app/data
    restart: unless-stopped
